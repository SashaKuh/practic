export const SearchBar = () => {
        return (
                <>
                        <input type="text" placeholder="Topuc filter" />
                        <select name="" id=""></select>
                </>
        )
}